import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import HelloWorld from '../HelloWorld.vue'

describe('HelloWorld', () => {
  it('renders properly', () => {
    
  })

  it('handles events properly', () => {
      expect(() => { throw new Error("apan") }).toThrowError()
  })

  it('xyzzz', () => {
    
  })
})
